---
# defaults file for docker_install
# global settings
pip_executable: pip3


# State of the docker package. Valid values: absent, present or latest
docker_package_state: present

# other docker settings
docker_pip_install: true # Will install the "docker" pip package

# docker_compose settings
docker_compose_install: true
docker_compose_version: latest

# A list of users who will be added to the docker group.
docker_users: []