galaxy_info:
  author: Lukas Menzel
  description: Docker install and configuration for Linux
  company: knelldev

  role_name: docker_install

  license: license (MIT)

  min_ansible_version: 1.2
  platforms:
  - name: Debian
    versions:
      - wheezy
      - jessie
      - stretch
      - buster
  - name: Ubuntu
    versions:
      - precise
      - trusty
      - xenial
      - bionic
      - focal

  galaxy_tags: 
    - linux
    - system
    - development
    - docker
    - container

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.